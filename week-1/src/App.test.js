// import { render, screen } from '@testing-library/react';
// import App from './App';

// If you are stellar write some tests ;)

test('true is true', () => {
  expect(true).toBe(true);
});

test('false is not true', () => {
  expect(false).not.toBe(true);
});
